import { createActions, createReducer } from 'reduxsauce';

const { Types, Creators } = createActions({
  showActiveProfilePopup:   null
})

export const MiscTypes = Types
export const MiscCreators = Creators
